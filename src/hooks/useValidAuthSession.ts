import { useAuthSession } from './useAuthSession';
import { useEffect } from 'react';

export const useValidAuthSession = () => {
  //   const { authSession } = useAuthSession();
  //
  //   useEffect(() => {
  //     console.log(`useEffect:useValidAuthSession :${isAuthenticated}`);
  //     (async () => {
  //       await authSession();
  //       console.log(`useEffect:useValidAuthSession :${isAuthenticated}`);
  //     })();
  //     // authSession().then((result) => console.log(result));
  //   }, [authSession]);
  //
  //   return { isAuthenticated };
};
